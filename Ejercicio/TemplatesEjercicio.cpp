#include "Tipos.hpp"
int main(){
    double tipoDouble;
    string tipoString;
    int tipoInt;
    EJMiClase tipoMiClase;
    Tipos(tipoDouble);
    Tipos(tipoString);
    Tipos(tipoInt);
    Tipos(tipoMiClase);   
}